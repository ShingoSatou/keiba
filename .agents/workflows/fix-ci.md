---
description: CI失敗ログを解析し、最小修正でruff/pytestを通す（原因→方針→修正→再検証）
---

CIログを読み、最小の修正でCIを通してください。

## 手順
1) 失敗の原因を1行で要約する（どのジョブ/どのエラーか）
2) 修正方針を箇条書きで示す（最小変更）
3) 修正する
4) 次をローカルで再実行し、結果を報告する  
   - uv run ruff format .
   - uv run ruff check .
   - uv run pytest -q
5) PR本文に追記できる形で「原因/対応/再実行結果」をまとめる