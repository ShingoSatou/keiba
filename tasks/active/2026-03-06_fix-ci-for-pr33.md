# 2026-03-06_fix-ci-for-pr33

## タイトル

- PR #33 の CI 失敗修正

## ステータス

- `active`

## 対象範囲

- `.github/workflows/ci.yml`
- CI で失敗している対象ファイル
- `tasks/todo.md`

## 対象バージョン

- `v3`

## 前提・仮定

- PR #33 の CI は `ruff check`, `ruff format --check`, `pytest -q` を実行する
- 失敗原因を特定してから、必要最小限の修正のみ入れる

## チェックリスト

- [x] 失敗ジョブを特定
- [x] 失敗原因を特定
- [x] 必要な修正を実装
- [x] ローカルで関連確認を再実行
- [ ] task file を done へ移動

## 確認結果

- `gh pr checks 33` で `test` job failed を確認
- `gh run view 22749826387 --json jobs` で失敗ステップが `Format (check)` だと確認
- failed log で `scripts_v3/feature_registry_v3.py`, `scripts_v3/train_binary_model_v3.py` の 2 本が `ruff format --check` 対象だと確認
- `uv run ruff format` で 2 本を整形
- `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest -q` はローカルで成功

## 実行コマンド

- `gh pr checks 33`
- `gh run view 22749826387 --job 65981820037 --log-failed`
- `gh run view 22749826387 --json jobs`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format scripts_v3/feature_registry_v3.py scripts_v3/train_binary_model_v3.py`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check .`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check .`
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q`

## 関連ドキュメント

- `.github/workflows/ci.yml`
- `tasks/todo.md`

## 残リスク

- GitHub 側の rerun が完了するまでは PR checks は pending
