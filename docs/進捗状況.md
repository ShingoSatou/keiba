# 進捗状況（仕様充足サマリ）: 2026-02-14 時点

この文書は「仕様書に書かれていることに対して、現時点でどこまで実装・検証・投入が終わっているか」を整理する。

* 対象プロジェクト: `keiba`
* 参照する一次ソース:
  * 取得: `docs/データ取得仕様書.md`
  * 取得対象: `docs/ダウンロードリスト.md`
  * ETL: `docs/ETL・パイプライン仕様書.md`, `scripts/load_to_db.py`
  * 正規化: `docs/データ辞書・正規化仕様書.md`, `app/infrastructure/parsers.py`, `init_db.sql`
  * 特徴量/学習: `init_mart.sql`, `scripts/build_features.py`, `scripts/build_dataset.py`, `scripts/train.py`, `scripts/backtest.py`

---

## 1. サマリ

### 1.1 目的（バックテストROIが出ない）に対する状況

* [x] `core.odds_final` が 0 件になる原因（`RACE` の `O1` が `SE` より先に来る入力順依存）を修正
* [x] `core.odds_final` を `raw.jv_raw` から復旧できるバックフィルスクリプトを追加
* [x] `scripts/backtest.py` が `core.odds_final` を参照して ROI/最大DD/購入回数まで出力できることを確認

### 1.2 “仕様”としての進捗（大枠）

* [x] **Backfillable（Model F相当）**: `DIFF` + `RACE` → core/mart → train/backtest の E2E が動作
* [ ] **0B41（時系列オッズ）を使う学習（Model M/O相当）**: 実装はあるが、DB投入・学習活用は未実施
* [ ] **運用当日T-5（as-ofスナップ）**: 仕様はあるが、mart側のスナップ/監査ログ/運用ジョブは未整備
* [ ] **検証設計（walk-forward等）**: `docs/検証設計仕様書.md` 準拠の実装は未整備

---

## 2. DB投入状況（スナップショット）

以下は `keiba_cp`（`.env` の `DATABASE_URL`）で確認した件数。

### 2.1 raw（取得済みJSONLの蓄積）

| 対象 | 件数 |
|---|---:|
| `raw.jv_raw` | 575,672 |
| `raw.jv_raw`（dataspec=`RACE`） | 353,755 |
| `raw.jv_raw`（dataspec=`DIFF`） | 221,917 |
| `raw.jv_raw`（dataspec=`0B41`） | 0 |

### 2.2 core（正規化）

| 対象 | 件数 |
|---|---:|
| `core.race` | 14,482 |
| `core.runner` | 153,313 |
| `core.result` | 150,603 |
| `core.payout` | 72,660 |
| `core.odds_final` | 100,547 |
| `core.horse` | 141,744 |
| `core.jockey` | 1,493 |
| `core.trainer` | 1,431 |
| `core.o1_header` / `core.o1_win`（0B41時系列） | 0 / 0 |
| `core.wh_header` / `core.wh_detail`（0B11馬体重） | 0 / 0 |
| `core.event_change`（0B16変更系） | 0 |
| `core.mining_dm` / `core.mining_tm`（MING/速報DM/TM） | 0 / 0 |
| `core.training_slop` / `core.training_wood` | 0 / 0 |
| `raw.jv_ck_event` / `core.ck_runner_event` / `mart.feat_ck_win`（CK） | 0 / 0 / 0 |

### 2.3 mart（特徴量）

| 対象 | 件数 |
|---|---:|
| `mart.run_index` | 99,730 |
| `mart.horse_stats` | 101,770 |
| `mart.person_stats` | 110,592 |

---

## 3. 仕様書別: 充足状況

### 3.1 取得（`docs/データ取得仕様書.md`）

* [x] 取得スクリプトの責務境界（JV-Link→JSONL）: `scripts/extract_jvlink.py`, `scripts/extract_rt_jvlink.py`
* [x] 実行前提（Windows + 32bit Python）を仕様として分離
* [ ] E2E（Windows環境での取得成功/例外系/再開性）はこのLinux環境では未検証

### 3.2 取得対象（`docs/ダウンロードリスト.md`）

Backfillable（2016〜現在）:
* [x] `DIFF`（UM/KS/CH）: 投入済み（master整備済み）
* [x] `RACE`（RA/SE/HR/O1）: 投入済み（最終オッズ含む）
* [ ] `MING`（DM/TM）: 未投入
* [ ] `SNPN`（CK）: 未投入
* [ ] `SLOP`/`WOOD`（調教）: 未投入
* [ ] `COMM`/`YSCH`（コース/開催）: 取得・取り込みとも未整備

Model M/O（0B41バックフィル）:
* [ ] `0B41`（O1時系列）: **未投入**（`raw.jv_raw` に 0 件）

Realtime-only（運用当日）:
* [ ] `0B11`（WH馬体重）: 実装あり・未投入
* [ ] `0B16`（WE/AV/JC/TC/CC）: 実装あり・未投入
* [ ] `0B13`/`0B17`（速報DM/TM）: 実装あり・未投入

### 3.3 ETL（`docs/ETL・パイプライン仕様書.md`）

* [x] 2パス（Pass1: raw + RA / Pass2: RA以外）で投入する基本設計
* [x] `raw.jv_raw` は `payload_hash` で重複排除する（DDLでUNIQUE、ローダでハッシュ計算）
* [x] `RACE` の `O1` は入力順により `SE` より先に出現するため、**遅延処理（Pass 2.5）**で `core.odds_final` を確実に埋める
* [x] 既存DB復旧のため `scripts/backfill_odds_final.py` を追加（raw→core再投入）

### 3.4 正規化（`docs/データ辞書・正規化仕様書.md`）

投入先（MVPで実際に利用している範囲）:
* [x] `RA` → `core.race`
* [x] `SE` → `core.runner` / `core.result`
* [x] `HR` → `core.payout`
* [x] `O1`（確定/最終） → `core.odds_final`
* [x] `UM/KS/CH` → `core.horse` / `core.jockey` / `core.trainer`

実装はあるが、未投入/未検証（データ0件）:
* [ ] `O1`（0B41時系列） → `core.o1_header` / `core.o1_win`
* [ ] `WH`（0B11） → `core.wh_header` / `core.wh_detail`
* [ ] `WE/AV/JC/TC/CC`（0B16） → `core.event_change`（mart反映はTODO）
* [ ] `DM/TM`（MING or 速報） → `core.mining_dm` / `core.mining_tm`
* [ ] `HC/WC` → `core.training_slop` / `core.training_wood`
* [ ] `CK`（SNPN） → `raw.jv_ck_event` / `core.ck_runner_event` / `mart.feat_ck_win`

### 3.5 特徴量/学習/バックテスト（`init_mart.sql`, `scripts/*`）

* [x] `scripts/build_features.py --rebuild` が完走し、`mart.*` が埋まる
* [x] `scripts/build_dataset.py --output data/train.parquet` が生成できる
* [x] `scripts/train.py` が学習できる（`models/lgb_model.pkl` が生成できる）
* [x] `scripts/backtest.py --use-test-split` が ROI/最大DD/購入回数を出せる（`core.odds_final` 参照）
* [ ] T-5 as-of 前提の mart スナップテーブル（運用監査込み）は未整備
* [ ] walk-forward（outer）等、検証設計仕様書準拠の学習/評価は未整備

---

## 4. 次にやること（優先度順の候補）

* P0: 0B41バックフィルの E2E（`0B41_*.jsonl` → `core.o1_*` が増える、冪等、誤ルーティング無し）
* P0: T-5スナップ（as-of）をDBで再現する mart テーブル設計と生成バッチ（運用/学習の共通土台）
* P1: SNPN（CK）投入の E2E（raw/core/mart まで）
* P1: MING（DM/TM）投入の E2E（学習に使えることを確認）
* P2: Realtime-only（0B11/0B16/0B13/0B17）を運用フローに接続（採用キー監査含む）

