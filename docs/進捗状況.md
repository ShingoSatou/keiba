# 進捗状況（仕様充足サマリ）: 2026-02-15 時点

この文書は「仕様書に書かれていることに対して、現時点でどこまで実装・検証・投入が終わっているか」を整理する。

* 対象プロジェクト: `keiba`
* 参照する一次ソース:
  * 取得: `docs/データ取得仕様書.md`
  * 取得対象: `docs/ダウンロードリスト.md`
  * ETL: `docs/ETL・パイプライン仕様書.md`, `scripts/load_to_db.py`
  * 正規化: `docs/データ辞書・正規化仕様書.md`, `app/infrastructure/parsers.py`, `migrations/0001_init_db.sql`
  * 特徴量/学習: `migrations/0002_init_mart.sql`, `scripts/build_features.py`, `scripts/build_dataset.py`, `scripts/train.py`, `scripts/backtest.py`

---

## 1. サマリ

### 1.1 目的（バックテストROIが出ない）に対する状況

* [x] `core.odds_final` が 0 件になる原因（`RACE` の `O1` が `SE` より先に来る入力順依存）を修正
* [x] `core.odds_final` を `raw.jv_raw` から復旧できるバックフィルスクリプトを追加
* [x] `scripts/backtest.py` が `core.odds_final` を参照して ROI/最大DD/購入回数まで出力できることを確認

### 1.2 “仕様”としての進捗（大枠）

* [x] **Backfillable（Model F相当）**: `DIFF` + `RACE` → core/mart → train/backtest の E2E が動作
* [ ] **0B41（時系列オッズ）を使う学習（Model M/O相当）**: ETL全件投入は完了（`core.o1_*` 反映・冪等・誤ルーティング無し、2026-02-15）。学習活用は未実施
* [x] **追加dataspecの取り込み（Backfillable）**: `MING`/`SNPN`/`SLOP`/`WOOD` は raw/core/mart まで投入・学習特徴として利用確認済み（2026-02-14）
* [ ] **運用当日T-5（as-ofスナップ）**: backfillable版のmart土台（`mart.t5_runner_snapshot` + 生成バッチ）は実装済み。Realtime-only採用キー反映（WH/0B16/速報DM/TM）は未実装
* [ ] **検証設計（walk-forward等）**: `docs/検証設計仕様書.md` 準拠の実装は未整備

---

## 2. DB投入状況（スナップショット）

以下は `keiba_cp`（`.env` の `DATABASE_URL`）で確認した件数。

### 2.0 取得済みJSONL（ローカルファイル）スナップショット

`data/`（実体: `/home/sato/projects/keibas/data`）に存在するJSONLの概況（DB投入有無は別）。

| dataspec | ファイル数 | 合計サイズ | 備考 |
|---|---:|---:|---|
| `RACE` | 5 | 26G | DB投入済み |
| `DIFF` | 1 | 384M | DB投入済み |
| `0B41` | 12,037 | 1.8G | DB投入済み（時系列O1、2026-02-15） |
| `0B11` | 11 | 44K | 未投入（WH） |
| `MING` | 1 | 20M | DB投入済み（DM/TM、2026-02-14） |
| `SNPN` | 1 | 3.3G | DB投入済み（CK、2026-02-14。`raw.jv_raw` はスキップ） |
| `SLOP` | 1 | 580M | DB投入済み（HC、2026-02-14） |
| `WOOD` | 1 | 115M | DB投入済み（WC、2026-02-14） |
| `COMM` | 1 | 888K | 未投入（CS。core化未整備） |
| `YSCH` | 1 | 2.2M | 未投入（YS。core化未整備） |
| **合計** | 12,060 | 32G |  |

※ `scripts/load_to_db.py` の `--input "data/0B41_*.jsonl"` のようなパターン指定は **必ずクォート**し、シェル展開させない（ファイル数が多い）。

### 2.1 raw（取得済みJSONLの蓄積）

| 対象 | 件数 |
|---|---:|
| `raw.jv_raw` | 11,475,759 |
| `raw.jv_raw`（dataspec=`RACE`） | 353,971 |
| `raw.jv_raw`（dataspec=`DIFF`） | 221,917 |
| `raw.jv_raw`（dataspec=`0B41`） | 5,357,220 |
| `raw.jv_raw`（dataspec=`MING`） | 68,580 |
| `raw.jv_raw`（dataspec=`SLOP`） | 4,781,204 |
| `raw.jv_raw`（dataspec=`WOOD`） | 692,867 |

### 2.2 core（正規化）

| 対象 | 件数 |
|---|---:|
| `core.race` | 14,494 |
| `core.runner` | 153,493 |
| `core.result` | 150,782 |
| `core.payout` | 72,788 |
| `core.odds_final` | 100,727 |
| `core.horse` | 141,919 |
| `core.jockey` | 1,493 |
| `core.trainer` | 1,431 |
| `core.schedule`（YSCH開催スケジュール） | 0 |
| `core.course`（COMMコース情報） | 0 |
| `core.o1_header` / `core.o1_win`（0B41時系列） | 5,355,452 / 74,936,225 |
| `core.wh_header` / `core.wh_detail`（0B11馬体重） | 0 / 0 |
| `core.event_change`（0B16変更系） | 0 |
| `core.mining_dm` / `core.mining_tm`（MING/速報DM/TM） | 487,038 / 471,868 |
| `core.training_slop` / `core.training_wood` | 4,777,100 / 692,865 |
| `raw.jv_ck_event` / `core.ck_runner_event` / `mart.feat_ck_win`（CK） | 491,523 / 491,523 / 491,523 |

### 2.3 mart（特徴量）

| 対象 | 件数 |
|---|---:|
| `mart.run_index` | 99,730 |
| `mart.horse_stats` | 101,770 |
| `mart.person_stats` | 110,592 |
| `mart.t5_runner_snapshot` | 180 |

### 2.4 P0サンプル検証（2016-01-05 / track=06 / 12R）

* 対象データ:
  * `data/RACE_subset_20160105_06.jsonl`（`RA/SE/HR/O1` を 12Rぶん抽出、216行）
  * `data/0B41_2016010506*.jsonl`（12ファイル）
* 実行:
  * `uv run python scripts/load_to_db.py --input data/RACE_subset_20160105_06.jsonl`
  * `uv run python scripts/load_to_db.py --input "data/0B41_2016010506*.jsonl"`（2回実行）
  * `uv run python scripts/build_t5_snapshot.py --date 2016-01-05`（2回実行）
* 検証結果:
  * `core.race` の対象12Rで `start_time`/`distance_m` が埋まる（RAパーサ修正の効果）
  * `core.o1_header`/`core.o1_win` が増加（12Rで `2,403 / 35,900`）
  * 0B41再投入でも件数が増えず冪等
  * 0B41投入前後で `core.odds_final`（対象12R）は `180` のまま（誤ルーティング無し）
  * `mart.t5_runner_snapshot` は 180行生成、`o1_announce_mmddhhmi <= to_char(asof_ts,'MMDDHH24MI')` 違反 0 件

---

## 3. 仕様書別: 充足状況

### 3.1 取得（`docs/データ取得仕様書.md`）

* [x] 取得スクリプトの責務境界（JV-Link→JSONL）: `scripts/extract_jvlink.py`, `scripts/extract_rt_jvlink.py`
* [x] 実行前提（Windows + 32bit Python）を仕様として分離
* [ ] E2E（Windows環境での取得成功/例外系/再開性）はこのLinux環境では未検証

### 3.2 取得対象（`docs/ダウンロードリスト.md`）

Backfillable（2016〜現在）:
* [x] `DIFF`（UM/KS/CH）: 投入済み（master整備済み）
* [x] `RACE`（RA/SE/HR/O1）: 投入済み（最終オッズ含む）
* [x] `MING`（DM/TM）: 投入済み（2026-02-14、`core.mining_*`）
* [x] `SNPN`（CK）: 投入済み（2026-02-14、`raw/core/mart`）
* [x] `SLOP`/`WOOD`（調教）: 投入済み（2026-02-14、`core.training_*`）
* [ ] `COMM`/`YSCH`（コース/開催）: 取得済み（`data/COMM_*.jsonl`, `data/YSCH_*.jsonl`）だが取り込み（core化）は未整備（ロードしてもraw止まり）

Model M/O（0B41バックフィル）:
* [x] `0B41`（O1時系列）: 全件投入/検証済み（2026-02-15。`raw.jv_raw`: 5,357,220件 / `core.o1_header`: 5,355,452件 / `core.o1_win`: 74,936,225件、`core.odds_final` への誤ルーティング増加なし）

Realtime-only（運用当日）:
* [ ] `0B11`（WH馬体重）: 実装あり・未投入（`data/0B11_*.jsonl` は存在）
* [ ] `0B16`（WE/AV/JC/TC/CC）: 実装あり・未投入
* [ ] `0B13`/`0B17`（速報DM/TM）: 実装あり・未投入

### 3.3 ETL（`docs/ETL・パイプライン仕様書.md`）

* [x] 2パス（Pass1: raw + RA / Pass2: RA以外）で投入する基本設計
* [x] `raw.jv_raw` は `payload_hash` で重複排除する（DDLでUNIQUE、ローダでハッシュ計算）
* [x] `RACE` の `O1` は入力順により `SE` より先に出現するため、**遅延処理（Pass 2.5）**で `core.odds_final` を確実に埋める
* [x] 既存DB復旧のため `scripts/backfill_odds_final.py` を追加（raw→core再投入）

### 3.4 正規化（`docs/データ辞書・正規化仕様書.md`）

投入先（MVPで実際に利用している範囲）:
* [x] `RA` → `core.race`
* [x] `SE` → `core.runner` / `core.result`
* [x] `HR` → `core.payout`
* [x] `O1`（確定/最終） → `core.odds_final`
* [x] `UM/KS/CH` → `core.horse` / `core.jockey` / `core.trainer`
* [x] `DM/TM`（MING） → `core.mining_dm` / `core.mining_tm`
* [x] `HC/WC`（SLOP/WOOD） → `core.training_slop` / `core.training_wood`
* [x] `CK`（SNPN） → `raw.jv_ck_event` / `core.ck_runner_event` / `mart.feat_ck_win`

実装はあるが、未投入/未検証（データ0件）:
* [x] `O1`（0B41時系列） → `core.o1_header` / `core.o1_win`（全件投入済み: 2026-02-15）
* [ ] `WH`（0B11） → `core.wh_header` / `core.wh_detail`（JSONL: `data/0B11_*.jsonl`）
* [ ] `WE/AV/JC/TC/CC`（0B16） → `core.event_change`（mart反映はTODO）

### 3.5 特徴量/学習/バックテスト（`migrations/0002_init_mart.sql`, `scripts/*`）

* [x] `scripts/build_features.py --rebuild` が完走し、`mart.*` が埋まる
* [x] `scripts/build_dataset.py --output data/train.parquet` が生成できる
* [x] `scripts/train.py` が学習できる（`models/lgb_model.pkl` が生成できる）
* [x] `scripts/backtest.py --use-test-split` が ROI/最大DD/購入回数を出せる（`core.odds_final` 参照）
* [ ] T-5 as-of 前提の mart スナップテーブル（backfillable版）は実装済み（`mart.t5_runner_snapshot`, `scripts/build_t5_snapshot.py`）。運用監査込み（WH/0B16/速報DM/TM）は未整備
* [ ] walk-forward（outer）等、検証設計仕様書準拠の学習/評価は未整備

---

## 4. 次にやること（優先度順の候補）

* [x] P0: 0B41バックフィルの **全件** E2E（完了: 2026-02-15。`data/0B41_ALL_20260215.jsonl` を投入し、`core.o1_*` 全件反映を確認）
* P0: T-5スナップ（as-of）の **Realtime-only拡張**（WH/0B16/速報DM/TM の採用キー反映）
* [x] P1: SNPN（CK）投入の E2E（`raw/core/mart` まで完了、2026-02-14）
* [x] P1: MING（DM/TM）投入の E2E（学習特徴として利用確認済み、2026-02-14）
* [x] P1: SLOP/WOOD（調教HC/WC）投入の E2E（学習特徴として利用確認済み、2026-02-14）
* P2: Realtime-only（0B11/0B16/0B13/0B17）を運用フローに接続（採用キー監査含む）
* P2: COMM/YSCH の core化（CS/YS → `core.course` / `core.schedule`）
