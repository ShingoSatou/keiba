# 進捗状況（仕様充足サマリ）: 2026-02-20 時点

この文書は「仕様書に書かれていることに対して、現時点でどこまで実装・検証・投入が終わっているか」を整理する。

* 対象プロジェクト: `keiba`
* 参照する一次ソース:
  * 取得: `docs/specs/10_データ取得仕様書.md`
  * 取得対象: `docs/specs/11_ダウンロードリスト.md`
  * ETL: `docs/specs/20_ETL仕様書.md`, `scripts/load_to_db.py`
  * 正規化: `docs/specs/22_データ辞書.md`, `app/infrastructure/parsers.py`, `migrations/0001_init_db.sql`
  * 特徴量/学習: `migrations/0002_init_mart.sql`, `scripts/build_features.py`, `scripts/build_dataset_t5.py`, `scripts/train_walk_forward.py`, `scripts/backtest_v2.py`
  * 運用T-5: `scripts/build_t5_snapshot.py`, `scripts/ops_t5_dryrun.py`, `scripts/ops_race.py`, `scripts/ops_rt_load.py`, `migrations/20260215_add_rt_mining_dm_tm.sql`, `migrations/20260215_event_change_dedup.sql`

---

## 1. サマリ

### 1.1 目的（バックテストROIが出ない）に対する状況

* [x] `core.odds_final` が 0 件になる原因（`RACE` の `O1` が `SE` より先に来る入力順依存）を修正
* [x] `core.odds_final` を `raw.jv_raw` から復旧できるバックフィルスクリプトを追加
* [x] `scripts/backtest_v2.py` が `core.odds_final` を参照して ROI/最大DD/購入回数まで出力できることを確認

### 1.2 “仕様”としての進捗（大枠）

* [x] **Backfillable（Model F相当）**: `DIFF` + `RACE` → core/mart → train/backtest の E2E が動作
* [x] **0B41（時系列オッズ）を使う学習（Model M/O相当）**: `scripts/build_dataset_t5.py` で T-5 + 0B41 時系列特徴（T-60〜T-5, 12点+派生）を実装。`scripts/train_walk_forward.py` / `scripts/train_production_bundle.py` で F/M/O + stacking 学習を実装（実データでの最終スコア確定は継続）
* [x] **追加dataspecの取り込み（Backfillable）**: `MING`/`SNPN`/`SLOP`/`WOOD` は raw/core/mart まで投入・学習特徴として利用確認済み（2026-02-14）
* [x] **運用当日T-5（as-ofスナップ）**: backfillable版のmart土台（`mart.t5_runner_snapshot` + 生成バッチ）は実装済み。Realtime-only拡張（WH/0B14/速報DM/TM の採用キー保存）と推論接続（`scripts/predict_t5.py`, `scripts/ops_t5_dryrun.py`）は実装済み。2026-02-20に `uv run python scripts/ops_t5_dryrun.py --race-date 20260215 --feature-set realtime --fetch-rt --fetch-rt-policy auto --fail-on-empty` を実行し、`data/ops/20260215/` に成果物一式（snapshot/predictions/audit/run_meta）を生成。`t5_snapshot_rows=506`、`predictions rows=506`、`health.after_snapshot.status=ok`、`odds_missing=0` を確認
* [x] **検証設計（walk-forward等）**: `scripts/train_walk_forward.py` で outer walk-forward + inner Train-fit/ES-valid/Calib + τ最適化（min bet ratio 10%）を実装

### 1.3 運用E2Eで判明した既知事項（2026-02-20更新）

* [x] `core.race` は FK回避のため `scripts/load_to_db.py` で stub（`surface=0`, `distance_m=0`）が作られる。`upsert_race` の `ON CONFLICT` を修正し、RA再投入で stub を上書きできるようにした（`surface/distance_m`）。
* [x] `scripts/ops_t5_dryrun.py` に `--fail-on-empty` を追加。`t5_snapshot rows=0` を運用失敗（非0終了）として扱えるようにした。
* [x] `scripts/ops_t5_dryrun.py` の day-health に `race_stub`（`surface=0 OR distance_m=0`）を追加し、`rows=0` の切り分けを容易化した。
* [x] JV-Link の戻り値 `-1` は「dataspec不正」ではなく「該当データ無し」。`scripts/extract_jvlink.py` / `scripts/extract_rt_jvlink.py` の表示を修正した。
* [x] `scripts/ops_t5_dryrun.py` に `--fetch-rt-policy auto`（`0B14=strict`, `0B41/0B11=warn`, `0B13/0B17=continue`）を追加し、dataspecごとに停止/継続を自動判定するようにした。
* [x] `scripts/ops_t5_dryrun.py` に `--ensure-race`（デフォルトON）を追加し、`core.race/core.runner` 前提不足時に `scripts/ops_race.py` を自動実行できるようにした。
* [x] `scripts/build_t5_snapshot.py` に `--ensure-race`（デフォルトON）を追加し、`missing_start_time` 比率の stop/warn 判定（`stop>=0.50`, `warn>0`）で前提不足日を `scripts/ops_race.py` で補完→再判定できるようにした（2026-02-18）。
* [x] `scripts/load_to_db.py` の `core.event_change` 投入を冪等化（`payload_md5` + `ON CONFLICT DO NOTHING`）し、`0B11` 投入で `ensure_race_stub` を補完するようにした。
* [x] RT投入分離コマンド `scripts/ops_rt_load.py` を追加し、dataspec別件数サマリを出力できるようにした。
* [x] `0B41(O1)` の `****` マスク値を欠損として扱うように修正（`OddsTimeSeriesRecord.parse`）。`build_t5_snapshot` / `predict_t5` で `odds<=0` も欠損判定に統一（2026-02-18）。
* [x] `build_t5_snapshot` 再実行時に対象日データを削除して再生成する動作は維持しつつ、削除は前提チェック通過後に実行する順序へ変更し、前提NG時に既存スナップを消さないようにした（2026-02-18）。
* [x] `ops_t5_dryrun` の day-health に snapshot 側のオッズ欠損件数（行/レース）を追加し、警告判定に反映（2026-02-18）。
* [x] 2026-02-20実行の full-run（`race_date=20260215`）で `0B41/0B14/0B11/0B13/0B17` の fetch/load がすべて `status=ok`。`core.o1_header(data_kbn=1)` は `36/36R`、`core.event_change=18`、`core.wh_header/core.wh_detail=36/125`、`core.rt_mining_dm/core.rt_mining_tm=506/494` を確認。
* [x] 同 run で `mart.t5_runner_snapshot` は `rows=506`、`odds_missing_flag=0`、`bw_source='WH'` は 86 行、`event_change_keys` は 506 行で非NULL。`t5_audit.json` で `race_field_overrides` が 23 レースに出力されることを確認。
* [x] 学習データ品質の不良を洗い出し、`scripts/check_dataset_quality.py` と `docs/ops/データ品質レポート.md` に整理（現行 `data/train.parquet` は placeholder混入や勝ち馬なしレース混入があり、モデル評価は未判定）。
* [x] 学習データ品質改善の実装を追加（`RaceRecord` の距離/0埋め補正、`upsert_race` の修復更新、`build_features.py` の 2016+ 範囲指定、`build_dataset.py` の `--min-horses`、`check_dataset_quality.py --gate`、`scripts/backfill_core_race.py`）。
* [ ] 速報系のうち `0B14/0B11/0B13/0B17` は開催日キー（`YYYYMMDD`）での取得が前提となる（または前提となるケースがある）ため、保持期間内データで実運用検証を継続する。

---

## 2. DB投入状況（スナップショット）

以下は `keiba_cp`（`.env` の `DATABASE_URL`）で確認した件数。

### 2.0 取得済みJSONL（ローカルファイル）スナップショット

`data/`（実体: `/home/sato/projects/keibas/data`）に存在するJSONLの概況（DB投入有無は別）。

| dataspec | ファイル数 | 合計サイズ | 備考 |
|---|---:|---:|---|
| `RACE` | 8 | 25.6G | DB投入済み |
| `DIFF` | 1 | 384M | DB投入済み |
| `0B41` | 37,497 | 10.9G | DB投入済み（時系列O1、2026-02-15） |
| `0B11` | 12 | 27K | DB投入済み（少量。WH） |
| `0B14` | 1 | 3.5K | DB投入済み（少量。WE/AV/JC/TC/CC） |
| `0B13` | 12 | 8.0K | DB投入済み（少量。速報DM） |
| `0B17` | 12 | 4.5K | DB投入済み（少量。速報TM） |
| `MING` | 1 | 20M | DB投入済み（DM/TM、2026-02-14） |
| `SNPN` | 1 | 3.3G | DB投入済み（CK、2026-02-14。`raw.jv_raw` はスキップ） |
| `SLOP` | 1 | 580M | DB投入済み（HC、2026-02-14） |
| `WOOD` | 1 | 115M | DB投入済み（WC、2026-02-14） |
| `COMM` | 1 | 888K | 未投入（CS。core化未整備） |
| `YSCH` | 1 | 2.2M | 未投入（YS。core化未整備） |
| **合計** | 37,549 | 40.8G |  |

※ `scripts/load_to_db.py` の `--input "data/0B41_*.jsonl"` のようなパターン指定は **必ずクォート**し、シェル展開させない（ファイル数が多い）。

### 2.1 raw（取得済みJSONLの蓄積）

| 対象 | 件数 |
|---|---:|
| `raw.jv_raw` | 12,832,473 |
| `raw.jv_raw`（dataspec=`RACE`） | 1,710,621 |
| `raw.jv_raw`（dataspec=`DIFF`） | 221,917 |
| `raw.jv_raw`（dataspec=`0B41`） | 5,357,220 |
| `raw.jv_raw`（dataspec=`0B11`） | 11 |
| `raw.jv_raw`（dataspec=`0B14`） | 31 |
| `raw.jv_raw`（dataspec=`0B13`） | 11 |
| `raw.jv_raw`（dataspec=`0B17`） | 11 |
| `raw.jv_raw`（dataspec=`MING`） | 68,580 |
| `raw.jv_raw`（dataspec=`SLOP`） | 4,781,204 |
| `raw.jv_raw`（dataspec=`WOOD`） | 692,867 |
| `raw.jv_raw`（dataspec=`COMM`） | 0 |
| `raw.jv_raw`（dataspec=`YSCH`） | 0 |

### 2.2 core（正規化）

| 対象 | 件数 |
|---|---:|
| `core.race` | 65,038 |
| `core.runner` | 741,567 |
| `core.result` | 732,212 |
| `core.payout` | 368,962 |
| `core.odds_final` | 488,884 |
| `core.horse` | 190,745 |
| `core.jockey` | 1,493 |
| `core.trainer` | 1,431 |
| `core.schedule`（YSCH開催スケジュール） | 0 |
| `core.course`（COMMコース情報） | 0 |
| `core.o1_header` / `core.o1_win`（0B41時系列） | 5,355,452 / 74,936,225 |
| `core.wh_header` / `core.wh_detail`（0B11馬体重） | 11 / 38 |
| `core.event_change`（0B14変更系） | 31 |
| `core.mining_dm` / `core.mining_tm`（MING/DM/TM） | 487,038 / 471,868 |
| `core.rt_mining_dm` / `core.rt_mining_tm`（0B13/0B17 速報DM/TM 履歴） | 165 / 165 |
| `core.training_slop` / `core.training_wood` | 4,777,100 / 692,865 |
| `raw.jv_ck_event` / `core.ck_runner_event` / `mart.feat_ck_win`（CK） | 491,523 / 491,523 / 491,523 |

### 2.3 mart（特徴量）

| 対象 | 件数 |
|---|---:|
| `mart.run_index` | 100,114 |
| `mart.horse_stats` | 102,272 |
| `mart.person_stats` | 110,592 |
| `mart.t5_runner_snapshot` | 484,229 |

### 2.4 P0サンプル検証（2016-01-05 / track=06 / 12R）

* 対象データ:
  * `data/RACE_subset_20160105_06.jsonl`（`RA/SE/HR/O1` を 12Rぶん抽出、216行）
  * `data/0B41_2016010506*.jsonl`（12ファイル）
* 実行:
  * `uv run python scripts/load_to_db.py --input data/RACE_subset_20160105_06.jsonl`
  * `uv run python scripts/load_to_db.py --input "data/0B41_2016010506*.jsonl"`（2回実行）
  * `uv run python scripts/build_t5_snapshot.py --date 2016-01-05`（2回実行）
* 検証結果:
  * `core.race` の対象12Rで `start_time`/`distance_m` が埋まる（RAパーサ修正の効果）
  * `core.o1_header`/`core.o1_win` が増加（12Rで `2,403 / 35,900`）
  * 0B41再投入でも件数が増えず冪等
  * 0B41投入前後で `core.odds_final`（対象12R）は `180` のまま（誤ルーティング無し）
  * `mart.t5_runner_snapshot` は 180行生成、`o1_announce_mmddhhmi <= to_char(asof_ts,'MMDDHH24MI')` 違反 0 件

---

## 3. 仕様書別: 充足状況

### 3.1 取得（`docs/specs/10_データ取得仕様書.md`）

* [x] 取得スクリプトの責務境界（JV-Link→JSONL）: `scripts/extract_jvlink.py`, `scripts/extract_rt_jvlink.py`
* [x] 実行前提（Windows + 32bit Python）を仕様として分離
* [ ] E2E（Windows環境での取得成功/例外系/再開性）はこのLinux環境では未検証

### 3.2 取得対象（`docs/specs/11_ダウンロードリスト.md`）

Backfillable（2016〜現在）:
* [x] `DIFF`（UM/KS/CH）: 投入済み（master整備済み）
* [x] `RACE`（RA/SE/HR/O1）: 投入済み（最終オッズ含む）
* [x] `MING`（DM/TM）: 投入済み（2026-02-14、`core.mining_*`）
* [x] `SNPN`（CK）: 投入済み（2026-02-14、`raw/core/mart`）
* [x] `SLOP`/`WOOD`（調教）: 投入済み（2026-02-14、`core.training_*`）
* [ ] `COMM`/`YSCH`（コース/開催）: 取得済み（`data/COMM_*.jsonl`, `data/YSCH_*.jsonl`）だが取り込み（core化）は未整備（ロードしてもraw止まり）

Model M/O（0B41バックフィル）:
* [x] `0B41`（O1時系列）: 全件投入/検証済み（2026-02-15。`raw.jv_raw`: 5,357,220件 / `core.o1_header`: 5,355,452件 / `core.o1_win`: 74,936,225件、`core.odds_final` への誤ルーティング増加なし）

Realtime-only（運用当日）:
* [x] `0B11`（WH馬体重）: 2026-02-20実行（`race_date=20260215`）で当日投入/採用を確認（`core.wh_header/core.wh_detail=36/125`, snapshot `bw_source='WH'`=86）
* [x] `0B14`（WE/AV/JC/TC/CC）: 2026-02-20実行（`race_date=20260215`）で当日投入/採用を確認（`core.event_change=18`, snapshot `event_change_keys` 非NULL=506, `t5_audit` の `race_field_overrides` 出力あり）
* [x] `0B13`/`0B17`（速報DM/TM）: 2026-02-20実行（`race_date=20260215`）で当日投入/採用を確認（`core.rt_mining_dm/core.rt_mining_tm=506/494`, snapshot `dm_kbn/tm_kbn` 非NULL=506/494）

### 3.3 ETL（`docs/specs/20_ETL仕様書.md`）

* [x] 2パス（Pass1: raw + RA / Pass2: RA以外）で投入する基本設計
* [x] `raw.jv_raw` は `payload_hash` で重複排除する（DDLでUNIQUE、ローダでハッシュ計算）
* [x] `RACE` の `O1` は入力順により `SE` より先に出現するため、**遅延処理（Pass 2.5）**で `core.odds_final` を確実に埋める
* [x] 既存DB復旧のため `scripts/backfill_odds_final.py` を追加（raw→core再投入）

### 3.4 正規化（`docs/specs/22_データ辞書.md`）

投入先（MVPで実際に利用している範囲）:
* [x] `RA` → `core.race`
* [x] `SE` → `core.runner` / `core.result`
* [x] `HR` → `core.payout`
* [x] `O1`（確定/最終） → `core.odds_final`
* [x] `UM/KS/CH` → `core.horse` / `core.jockey` / `core.trainer`
* [x] `DM/TM`（MING） → `core.mining_dm` / `core.mining_tm`
* [x] `HC/WC`（SLOP/WOOD） → `core.training_slop` / `core.training_wood`
* [x] `CK`（SNPN） → `raw.jv_ck_event` / `core.ck_runner_event` / `mart.feat_ck_win`

実装 + 当日E2E確認済み（2026-02-20）:
* [x] `O1`（0B41時系列） → `core.o1_header` / `core.o1_win`（全件投入済み: 2026-02-15）
* [x] `WH`（0B11） → `core.wh_header` / `core.wh_detail`（2026-02-20実行の `race_date=20260215` full-run で当日E2E採用を確認）
* [x] `WE/AV/JC/TC/CC`（0B14） → `core.event_change`（`scripts/build_t5_snapshot.py` で TC/AV/JC/WE/CC の as-of 採用キー保存、`scripts/predict_t5.py` で WE/CC を `distance_m/surface/going/weather` へ実効反映。2026-02-20実行の `race_date=20260215` full-run で当日E2E採用を確認）

### 3.5 特徴量/学習/バックテスト（`migrations/0002_init_mart.sql`, `scripts/*`）

* [x] `scripts/build_features.py --rebuild` が完走し、`mart.*` が埋まる
* [x] `scripts/build_dataset_t5.py --output data/train.parquet` が生成できる
* [x] `scripts/train_walk_forward.py` が学習できる（`models/lgb_model.pkl` が生成できる）
* [x] `scripts/backtest_v2.py --use-test-split` が ROI/最大DD/購入回数を出せる（`core.odds_final` 参照）
* [x] T-5 as-of 前提の mart スナップテーブル（backfillable版）は実装済み（`mart.t5_runner_snapshot`, `scripts/build_t5_snapshot.py`）。Realtime-only採用キー保存は WH/TC/AV/JC/WE/CC/速報DM/TM まで実装済み。`scripts/ops_t5_dryrun.py` で推論（`scripts/predict_t5.py`）と買い目表/監査ログ出力まで接続済み。監査は run/race の欠損率サマリ・採用キー参照情報・`race_field_overrides` を出力する形へ拡張済み。2026-02-20実行の `race_date=20260215` full-run で実データE2E（成果物生成と採用キー反映）を確認
* [x] walk-forward（outer）等、検証設計仕様書準拠の学習/評価スクリプトを追加（`scripts/train_walk_forward.py`）

---

## 4. 次にやること（優先度順）

### P0: Realtime-only（運用当日）E2Eを1日分「運用OK」まで完了（2026-02-20達成）

直近の観測（2026-02-20実行、`race_date=20260215`, `feature_set=realtime`）:

* `ops_t5_dryrun` full-run が成功（`--fetch-rt --fetch-rt-policy auto --fail-on-empty`）
* 取得/投入は `0B41/0B14/0B11/0B13/0B17` すべて `status=ok`
* `core.o1_header(data_kbn=1)` は `36/36R`、`mart.t5_runner_snapshot` は `rows=506`・`odds_missing_flag=0`
* `data/ops/20260215/` に `t5_snapshot.*` / `t5_predictions.*` / `t5_audit.json` / `run_meta.json` を出力（`t5_predictions.csv` は 506 行）

やること:

* [x] `ops_t5_dryrun` で full-run を回す（`uv run python scripts/ops_t5_dryrun.py --race-date 20260215 --feature-set realtime --fetch-rt --fetch-rt-policy auto --fail-on-empty`）
  * 受け入れ: `data/ops/20260215/` に `t5_snapshot.*` / `t5_predictions.*` / `t5_audit.json` が揃い、`t5_snapshot_rows=506` かつ `predictions rows=506`
* [x] 0B41（T-5オッズ）: 当日36Rすべてに `core.o1_header(data_kbn=1)` が存在（`36/36R`）
  * 受け入れ: `mart.t5_runner_snapshot` の `odds_missing_flag=0`（`<=0.50` を満たす）
* [x] 0B14（変更系）: 当日分の `core.event_change` を投入し、`event_change_keys` と `race_field_overrides` が出ることを確認（`core.event_change=18`, `event_change_keys` 非NULL=506, `race_field_overrides` 出力レース=23）
* [x] 0B11（WH）: 当日分の `core.wh_*` を投入し、`bw_source='WH'` が増えることを確認（`core.wh_header/core.wh_detail=36/125`, `bw_source='WH'`=86）
* [x] 0B13/0B17（速報DM/TM）: 当日分の `core.rt_mining_*` を投入し、`dm_kbn/tm_kbn` が埋まることを確認（`core.rt_mining_dm/core.rt_mining_tm=506/494`, snapshot `dm_kbn/tm_kbn` 非NULL=506/494）

### P1: 運用ゲート（stop/warn）を確定

* [ ] `snapshot_odds_missing` 高比率を warn のままにするか stop にするか決める（現状は `threshold=0.50` で warn）
* [ ] `missing_start_time` stop/warn しきい値を運用仕様として明記（`build_t5_snapshot` と `ops_t5_dryrun` で整合）
* [ ] 学習用 `data/train.parquet` を再生成して品質ゲート合格（`--gate` は実装済み。実データで PASS を確認して完了化）

### P2: 拡張

* [ ] COMM/YSCH の core化（CS/YS → `core.course` / `core.schedule`）
