# 実装ギャップ/ToDo（仕様 ↔ 実装 ↔ DB）

> この文書は**未完了TODOのみ**を管理する。完了済みの項目は削除している。
> 完了記録は `docs/ops/進捗状況.md` を参照。

* 対象プロジェクト: `keiba`
* 最終更新: 2026-02-22
* 優先度: **P0**=ブロッカー / **P1**=重要 / **P2**=低優先

**仕様書（参照）**:
- `docs/specs/00_全体仕様書.md`, `docs/specs/10_データ取得仕様書.md`
- `docs/specs/20_ETL仕様書.md`, `docs/specs/21_DB設計仕様書.md`, `docs/specs/22_データ辞書.md`
- `docs/specs/30_特徴量仕様書.md`, `docs/specs/31_特徴量カタログ.md`
- `docs/specs/40_検証設計仕様書.md`, `docs/specs/50_運用仕様書.md`

---

## P0 ブロッカー

### DB: 制約未適用時の対応
- [ ] 既存DBに `raw.jv_raw` の UNIQUE制約（`payload_hash`）が無い場合は `migrations/20260213_raw_dedup.sql` を適用する
- [ ] `uv run python scripts/load_to_db.py --input-dir data/` が「テーブルが無い/制約が無い」で落ちないことを確認する

### ETL: `core.race` のstub残存確認
- [ ] 既存stubが残る開催日を対象に、RACE再投入で `surface>0` かつ `distance_m>0` に復元されることを確認する
- [ ] `core.race` の運用対象日で `surface=0 OR distance_m=0` が許容値以下（理想0）

### 特徴量: T-5スナップ E2E検証（Realtime-only）
- [ ] Realtime-only（0B11/0B14/0B13/0B17）を含めた実データE2Eで T-5スナップ再現できることを確認する

### 学習・検証: 再現可能な検証レポート生成
- [ ] 仕様書の分割ルールで、再現可能な検証レポート（Logloss/AUC/ROI/DD等）が出せることを確認する

---

## P1 重要

### DM/TM: 運用E2E検証
- [ ] 運用データ（0B13/0B17）でE2E検証し、T-5採用キーが期待どおり入ることを確認する
- [ ] 必要なら `dm_deviation` 等、DDLにあるのに未投入の列を埋める（仕様と照合）
- [ ] 当日運用で、T-5以前の「最新DM/TM」を仕様通りに採用できる（採用キーが残る）

### 当日変更: 監査キー追跡
- [ ] 同一レースで複数イベントが来ても、採用した状態が再現できる（監査キーが追える）

### 調教（SLOP/WOOD）: martへの集約
- [ ] martに集約テーブルを追加し、`scripts/build_features.py` で生成する（必要なら）

### ドキュメント整合
- [ ] `README.md` の「取得・ロード」の説明を仕様書（取得=JSONLまで）に合わせる

### モデル: 再現可能な推論パイプライン
- [ ] 同一コード/同一入力で、再現可能に `pF/pM/ô/p_final/EV_hat` が生成される

---

## P2 低優先

### COMM/YSCH の投入（コース/開催スケジュール）
- [ ] 必要な `rec_id`（`CS`, `YS` 等）をパースするパーサを追加するか判断する
- [ ] 当面は「取得できるが投入しない」を仕様として固定するか（`docs/specs/10_データ取得仕様書.md` またはREADMEに明記）

---

## 最低限の検証（実装修正後のチェック用）

- [ ] `uv run python scripts/verify_parsers.py`
- [ ] サンプルデータでロード確認（`--input-dir data/`）
- [ ] DB側での簡易サニティ
  - [ ] `core.o1_header/core.o1_win` に `0B41` が入っている
  - [ ] `core.mining_*` に `horse_no != 0` が入っている
  - [ ] `raw.jv_ck_event` が増える（SNPN投入ができている）
- [ ] 特徴量・学習（現行MVPの動作確認）
  - [ ] `uv run python scripts/build_features.py`
  - [ ] `uv run python scripts/build_dataset_t5.py`
  - [ ] `uv run python scripts/train_walk_forward.py --no-wandb`
  - [ ] `uv run python scripts/backtest_v2.py --use-test-split`

---

## 旧 DB構築ToDo（統合済み）

以下は旧 `docs/DB構築ToDo.md` から移管した未完了項目：

- [ ] **raw.jv_raw の冪等化（payload_hash）を既存DBに適用** — `migrations/20260213_raw_dedup.sql` 参照
- [ ] **全データのロードテスト** — Backfillable（JVOpen + 0B41バックフィル）で 2016〜現在の学習DBが作れることを先に検証する。Realtime-only（0B11/0B14/0B13/0B17）は運用開始以降のデータが揃い次第、別途E2Eで検証する。
