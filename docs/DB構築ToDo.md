# DB構築・ロード実装状況とToDo

## 1. 現状概要

主要なテーブル定義（Schema）とデータ投入ロジック（Loader）は概ね実装が完了しています。
ドキュメント上の「未実装・未接続」の記述の一部は、実際には実装済みであり、ドキュメントの更新が必要です。

### 実装状況サマリ

| データ種別 | RecID | DB Schema | Parser | Loader | 状態 / 備考 |
|---|---|---|---|---|---|
| レース詳細 | RA | ✅ | ✅ | ✅ | 実装完了 |
| 出走馬・結果 | SE | ✅ | ✅ | ✅ | 実装完了 |
| 払戻金 | HR | ✅ | ✅ | ✅ | 実装完了 |
| 確定オッズ | O1 | ✅ | ✅ | ✅ | 実装完了 |
| 時系列オッズ | O1(0B41) | ✅ | ✅ | ✅ | **ローダ実装済み・データ未取得** |
| マスタ（馬/騎手/調教師） | UM/KS/CH | ✅ | ✅ | ✅ | 実装完了 |
| 競走馬除外 | JG | ✅ | ✅ | ✅ | 実装完了（馬名更新のみ） |
| 馬体重速報 | WH | ✅ | ✅ | ✅ | 実装完了 |
| 調教（坂路/ウッド） | HC/WC | ✅ | ✅ | ✅ | 実装完了 |
| マイニング | DM/TM | ✅ | ✅ | ✅ | 実装完了 |
| 出走別着度数（CK） | CK | ✅ | ✅ | ⚠️ | **ロジック整理が必要**（動作はする見込み） |
| 当日変更情報 | WE/AV等 | ✅ | ✅ | ✅ | 実装完了（Raw保存のみ） |
| 速報オッズ | O1(0B15) | ✅ | ✅ | ⚠️ | **0B41以外は上書き保存**（時系列保持なし） |
| 速報票数 | H1/H6(0B12) | ❌ | ❌ | ❌ | **完全未実装**（必要なら追加要） |

## 2. 実装ギャップとToDoリスト

DB構築を完了し、信頼できる状態にするためのタスクリストです。

### [Code] コード修正・整理

- [ ] **リアルタイム系データ取得ツールの拡張** <!-- id: rt-tool-update -->
    - `scripts/extract_rt_jvlink.py` は現在単一レース指定のみ対応しています。
    - 過去分（時系列オッズ 0B41 等）をまとめて取得するために、期間指定でループ実行するラッパーまたは機能拡張が必要です。
    - **[追加]** 必要に応じて速報票数（0B12）等の取得機能・パーサー実装も検討が必要です。

- [ ] **CKロードロジックの整理 (`scripts/load_to_db.py`)** <!-- id: ck-clean -->
    - `insert_ck_record` 関数内に、開発時のメモ（`# I'll rely on ...` 等）やコメントアウトされた試行錯誤のコードが残っています。
    - ロジック自体は実装されていますが、可読性と保守性のために不要なコメントを削除し、コードを整理する必要があります。

### [Doc] ドキュメント更新

- [ ] **仕様書のステータス更新 (`docs/データ辞書・正規化仕様書.md`)** <!-- id: doc-update -->
    - `5.5 O1（Time Series: 0B41）` の「⚠️（ローダ未接続）」を「✅（実装済み）」に更新。
    - `5.10` の `event_change` について、現状の実装（`{"raw": ...}` 形式でのJSON保存）を仕様として明記するか、構造化を行う（後述）かの方針を確定。
    - `CK` の実装状況を更新。

### [Test] 検証

- [ ] **全データのロードテスト**
    - 全データスペックを含むJSONLファイルを用いて、`load_to_db.py` がエラーなく完走することを確認する。
    - **注記**: 時系列オッズ（0B41）はデータ未取得のため、取得ツール拡張後に検証を行う必要があります。

### [Future] 将来的な改善（Optional）

- [ ] **イベント変更情報の構造化**
    - 現在 `payload_parsed` には `raw` 文字列しか入っていないため、`EventChangeRecord` で詳細なパースを行い、JSONBへの構造化保存を実装する（分析や表示で使いやすくするため）。

## 3. 次のアクション

1.  `scripts/load_to_db.py` の `insert_ck_record` をクリーンアップする。
2.  `docs/データ辞書・正規化仕様書.md` を更新し、実装状態と一致させる。
3.  手元のデータを使ってロードテストを実行する。
